"""Subcommand implementations.

One module per command keeps ``cli.py`` a wiring file you can read in a minute, and
means a command's tests, imports and optional dependencies stay next to the command
that owns them.
"""

from __future__ import annotations

__all__: list[str] = []
